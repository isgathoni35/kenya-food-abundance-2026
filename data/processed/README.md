# Processed Outputs

These files are generated by the prediction pipeline in `src/prediction_pipeline.py`:

- `model_training_table.csv`
- `2026_predictions.csv`
- `model_coefficients.csv`
- `validation_metrics.json`

Run the pipeline after adding real raw datasets to `data/raw/`:

```bash
python -m src.prediction_pipeline
```
